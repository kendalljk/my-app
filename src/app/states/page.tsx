import React from "react";
import Navigation from "../components/Navigation";

const State = () => {
    return (
        <section>
            <Navigation />
            <div>State</div>
        </section>
    );
};

export default State;
